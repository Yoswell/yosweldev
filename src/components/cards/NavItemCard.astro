---
import Anchor from '@/components/utils/Anchor.astro'

const { ...props } = Astro.props
---

<Anchor
    href={ props.href }
    aria-label={`Loaded internal page ~ ${ props.href }`}
    class={ Astro.props.class }>

    <slot />
    
    <div>
        <h6 class='text-white text-sm'>
            { props.head }
        </h6>
        <p class='block text-(--fg2) text-xs whitespace-nowrap'>
            { props.content }
        </p>
    </div>
</Anchor>
